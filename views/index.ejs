<%- include('partials/header') -%>

<div class="container pt-5">
  <div class="row row-cols-1 row-cols-md-3 p-3">
    <% for( let i = 0; i < searchRecipes.results.length; i++ ) { %>
    <div class="col mb-4">
      <div>
        <a href="/recipes/<%= searchRecipes.results[i].id %>">
          <img
            src="https://spoonacular.com/recipeImages/<%=  searchRecipes.results[i].id %>-312x231.<%= searchRecipes.results[i].imageType %>"
            class="card-img-top"
            alt="..."
          />
        </a>
        <div class="card-body">
          <h5 class="card-title">
            <a href="/recipes/<%= searchRecipes.results[i].id %>"
              ><%= searchRecipes.results[i].title %>
            </a>
          </h5>
        </div>
      </div>
    </div>
    <% } %>
  </div>
</div>

<%- include('partials/footer') -%>
